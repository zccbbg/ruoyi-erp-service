<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.erp.basic.mapper.ItemSkuMapper">

    <resultMap id="skuMapVo" type="com.ruoyi.erp.basic.domain.vo.SkuMapVo">
        <association property="goods" javaType="com.ruoyi.erp.basic.domain.vo.ItemVo">
            <id property="id" column="item_id"/> <!-- 映射 goods 的 id -->
        </association>
        <association property="sku" javaType="com.ruoyi.erp.basic.domain.vo.SkuVo">
            <id property="id" column="sku_id"/> <!-- 映射 goods 的 id -->
        </association>
    </resultMap>

    <select id="queryItemSkuMapVos" resultMap="skuMapVo">
        select
            sku.id as sku_id,
            sku.*,
            goods.id as item_id,
            goods.*
        from wms_item_sku sku
        inner join wms_item goods on sku.item_id=goods.id
        where sku.id in
        <foreach collection="ids" goods="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="queryItemSkuMapVo" resultMap="skuMapVo">
        select
        sku.id as sku_id,
        sku.*,
        goods.id as item_id,
        goods.*
        from wms_item_sku sku
        inner join wms_item goods on sku.item_id=goods.id
        where sku.id = #{id}
    </select>

    <select id="selectByBo" resultMap="skuMapVo">
        select
            sku.id as sku_id,
            sku.*,
            goods.id as item_id,
            goods.*
        from wms_item_sku sku
        inner join wms_item goods on sku.item_id=goods.id
        inner join wms_item_category category on goods.item_category=category.id
        <where>
            <if test="bo.itemName != null and bo.itemName != ''">
                and goods.item_name like concat('%', #{bo.itemName}, '%')
            </if>
            <if test="bo.itemCode != null and bo.itemCode != ''">
                and goods.item_code like concat('%', #{bo.itemCode}, '%')
            </if>
            <if test="bo.itemBrand != null">
                and goods.item_brand=#{bo.itemBrand}
            </if>
            <if test="bo.skuName != null and bo.skuName != ''">
                and sku.sku_name like concat('%', #{bo.skuName}, '%')
            </if>
            <if test="bo.barcode != null and bo.barcode != ''">
                and sku.barcode like concat('%', #{bo.barcode}, '%')
            </if>
            <if test="bo.itemCategory != null and bo.itemCategory != ''">
                and (category.id=#{bo.itemCategory} or category.parent_id=#{bo.itemCategory})
            </if>
        </where>
        order by sku.item_id desc
    </select>
</mapper>
